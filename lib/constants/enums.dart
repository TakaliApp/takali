/// [ViewModelState]
/// Enum used to manage a viewmodel's state directly from the viewmodel.
enum ViewModelState {
  busy,
  idle,
}

/// [LocalBoxes]
/// enum used to manage local Storage boxes
enum LocalBoxes {
  settings,
  profile,
}
